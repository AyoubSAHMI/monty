#include "monty.h"
/**
* nop - a function that does nothing
* @stack: an unused parameter
* @line_number: an unused parameter
* Return: nothing
*/
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
